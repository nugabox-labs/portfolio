# AGENTS.md

이 저장소에서 작업하는 모든 에이전트(및 사람)가 따라야 하는 운영 규칙.

## 버전 관리

- Semantic Versioning을 따르며 `v0.1.0`부터 시작한다.
- 기능이 하나 구현될 때마다 minor 버전을 올린다. 버그 수정은 patch.
- 버전을 올릴 때마다 `CHANGELOG.md`를 갱신한다.

## 커밋 규칙

- Conventional Commits 형식을 사용한다 (`feat:`, `fix:`, `chore:`, `docs:`, `content:` 등).
- 기능 단위로 작게 커밋한다.
- 기능이 완료되면 즉시 커밋 + push한다. 작업을 누적해두지 않는다.

## 변경 보고

- 매 수정 후 한국어로 무엇을·왜 바꿨는지 간략히 보고한다. 조용히 다음 단계로 넘어가지 않는다.

## Docker Compose 원칙

- `./compose.sh up` / `down` = 운영(prod). `docker-compose.yml`, standalone 빌드 기반이며 코드 변경 후에는 재빌드가 필요하다.
- `./compose.sh --dev up` / `down` = 개발(dev). `docker-compose.dev.yml`, 소스 바인드 마운트 + `next dev` 핫리로드로 즉시 반영된다.
- dev/prod는 포트와 네트워크를 분리해 서로 간섭하지 않게 한다.

## Notion 원칙

- 콘텐츠(소개/경력/학력/수상/프로젝트)의 원본은 Notion 데이터베이스이다.
- "공개" 체크박스가 사이트 노출 여부를 결정하는 단일 기준이다.
- 템플릿 코드에 콘텐츠를 직접 하드코딩하지 않는다 (레이아웃/스타일 코드는 예외).

## 비밀정보

- `.env`는 절대 커밋하지 않는다.
- 프로덕션 이미지 레이어에 토큰이 남지 않도록 빌드 타임이 아닌 런타임 주입을 우선한다.

## 불확실성 처리

- Next.js/Notion API의 동작이 불확실하면 지어내지 말고 공식 문서를 확인하거나 사용자에게 질문한다.
